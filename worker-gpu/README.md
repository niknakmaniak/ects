# ECTS GPU Worker

## Install (2 clics)

1. **install.bat** — telecharge Python 3.12 dans `python312/` (local, sans toucher Windows)
2. Edite **.env** (token GPU du VPS)
3. **run.bat** — laisse la fenetre ouverte

Pas de Docker. Pas de winget. Compatible meme si tu as Python 3.15 sur le PC.
